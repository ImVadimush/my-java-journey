# 🟢 Git - cheat sheet

## 🔹 Рекомендованный workflow
```bash
git status – проверить изменения
git add . – добавить изменения
git commit -m "описание" – создать коммит
git push origin main – отправить на GitHub
```

## 🔹 Статус и история
```bash
git status       # что изменено и staged
git log          # история коммитов
git diff         # различия 
```

## 🔹 Добавление изменений
```bash
git add <файл>   # добавить конкретный файл
git add .        # добавить все изменения
```

## 🔹 Коммиты
```bash
git commit -m "описание"        # создать коммит
git commit --amend -m "новое"   # исправить последний коммит
```

## 🔹 Push / Pull
```bash
git push origin main       # отправка коммитов
git push -u origin main    # первый push, ставит tracking ветку
git pull origin main       # забрать изменения с GitHub
git push -n origin main    # dry run, показать что будет
```

## 🔹 Remote
```bash
git remote -v              # показать remote
git remote add name url    # добавить новый remote
git remote set-url origin url  # изменить URL
```

## 🔹 Ветки
```bash
git branch        # показать локальные ветки
git branch имя    # создать новую ветку
git checkout имя  # переключиться на ветку
git merge имя     # объединить ветку с текущей
```